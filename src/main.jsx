import React from 'react'
import ReactDOM from 'react-dom/client'
import FrontEndVerde from './FrontEndVerde'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FrontEndVerde />
  </React.StrictMode>,
)
